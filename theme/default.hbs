<!DOCTYPE html>
<html lang="{{@site.locale}}">
<head>
	<title>{{meta_title}}</title>
	<meta charset="utf-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />

	{{!-- preloads --}}
	<link rel="preconnect" href="https://fonts.openlab.dev/" />
	<link rel="preconnect" href="https://alembic.openlab.dev/" />

	{{!-- styles --}}
	<link rel="stylesheet" href="https://alembic.openlab.dev/everything.css">
	<link rel="stylesheet" href="https://fonts.openlab.dev/inter/inter.css">
	<link rel="stylesheet" href="{{asset "screen.css"}}">
	
	{{!-- This tag outputs all your advanced SEO meta, structured data, and other important settings,
	it should always be the last tag before the closing head tag --}}
	{{ghost_head}}
</head>
<body class="{{body_class}}">
	<header class="siteHeader">
		<sidebar-layout>
			<cluster-layout justify="space-between" align="center" space="var(--s-3)">
				<a href="/"><img src="/assets/brand-dark.png" height="42"></a>
				<nav>
					{{navigation}}
				</nav>
			</cluster-layout>
		</sidebar-layout>
	</header>
	{{{body}}}
	<footer class="siteFooter">
		<center-layout>
			<cluster-layout justify="space-between">
				<span>{{@site.title}}</span>
				<nav>
					{{navigation type="secondary"}}
				</nav>
			</cluster-layout>
		</center-layout>
	</footer>

	<script type="module" src="https://alembic.openlab.dev/everything.js"></script>

	{{!-- Ghost outputs required functional scripts with this tag - it should always be the last thing before the closing body tag --}}
	{{ghost_foot}}
</body>
</html>

{{> 'empty'}}
